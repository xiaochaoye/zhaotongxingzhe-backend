#线上环境是怎么样就怎么样填
spring:
  # DataSource Config
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    #线上数据库url
    url: jdbc:mysql://localhost:3306/zhaotongxingzhe
    username: root
    password: 你的线上数据库密码
  # session 失效时间
  session:
    timeout: 86400
server:
  address: 0.0.0.0
